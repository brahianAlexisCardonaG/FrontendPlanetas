import { RemoveSecurityImgPipe } from './remove-security-img.pipe';

describe('RemoveSecurityImgPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSecurityImgPipe();
    expect(pipe).toBeTruthy();
  });
});
